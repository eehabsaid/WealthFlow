# WealthFlow Database Schema & Model Reference

> **Auto-Generated Documentation** — Last synchronized: `2026-08-07 17:00:22 UTC`
> Do not edit this file manually. Run `python manage.py generate_ai_knowledge` to update.

## Overview
This document provides the authoritative, introspected database schema for WealthFlow Django models.

## App: `AUTH`

### Model: `Group` (Table: `auth_group`) 
**Description**: Groups are a generic way of categorizing users to apply permissions, or

#### Fields:
- `id` (AutoField)
- `name` (CharField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `Permission` (Table: `auth_permission`) 
**Description**: The permissions system provides a way to assign permissions to specific

#### Fields:
- `id` (AutoField)
- `name` (CharField)
- `content_type` (ForeignKey -> ContentType): FK relation to `ContentType`.
- `codename` (CharField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `User` (Table: `auth_user`) 
**Description**: Users within the Django authentication system are represented by this

#### Fields:
- `id` (AutoField)
- `password` (CharField)
- `last_login` (DateTimeField)
- `is_superuser` (BooleanField) — Designates that this user has all permissions without explicitly assigning them.
- `username` (CharField) — Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.
- `first_name` (CharField)
- `last_name` (CharField)
- `email` (CharField)
- `is_staff` (BooleanField) — Designates whether the user can log into this admin site.
- `is_active` (BooleanField) — Designates whether this user should be treated as active. Unselect this instead of deleting accounts.
- `date_joined` (DateTimeField)

#### Calculated Properties:
- `is_anonymous` (property): Always return False. This is a way of comparing User objects to
anonymous users.
- `is_authenticated` (property): Always return True. This is a way to tell if the user has been
authenticated in templates.
- `pk` (property): Calculated property

## App: `CORE`

### Model: `AIBenchmarkReport` (Table: `core_aibenchmarkreport`) 
**Description**: Stores 7-dimension benchmark evaluation results comparing candidate models vs active production.

#### Fields:
- `id` (BigAutoField)
- `model_version` (ForeignKey -> AIModelVersion): FK relation to `AIModelVersion`.
- `business_analysis_score` (FloatField)
- `financial_reasoning_score` (FloatField)
- `architecture_score` (FloatField)
- `code_understanding_score` (FloatField)
- `feature_suggestion_score` (FloatField)
- `hallucination_resistance_score` (FloatField)
- `instruction_following_score` (FloatField)
- `overall_score` (FloatField)
- `passed_promotion_gate` (BooleanField)
- `evaluation_details_json` (TextField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AIConversation` (Table: `core_aiconversation`) 
**Description**: AIConversation(id, user, title, is_deleted, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `user` (ForeignKey -> User): FK relation to `User`.
- `title` (CharField)
- `is_deleted` (BooleanField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AIKnowledgeEntry` (Table: `core_aiknowledgeentry`) 
**Description**: Distilled, verified long-term domain knowledge entry.

#### Fields:
- `id` (BigAutoField)
- `key` (CharField)
- `category` (CharField)
- `title` (CharField)
- `content` (TextField)
- `confidence` (FloatField)
- `source` (CharField)
- `is_active` (BooleanField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AIMessage` (Table: `core_aimessage`) 
**Description**: AIMessage(id, conversation, role, content, sources, tool_calls, prompt_tokens, completion_tokens, is_deleted, created_at)

#### Fields:
- `id` (BigAutoField)
- `conversation` (ForeignKey -> AIConversation): FK relation to `AIConversation`.
- `role` (CharField)
- `content` (TextField)
- `sources` (JSONField)
- `tool_calls` (JSONField)
- `prompt_tokens` (IntegerField)
- `completion_tokens` (IntegerField)
- `is_deleted` (BooleanField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AIModelVersion` (Table: `core_aimodelversion`) 
**Description**: Stores local AI model versions, training metadata, and active status.

#### Fields:
- `id` (BigAutoField)
- `version_name` (CharField)
- `base_model` (CharField)
- `training_backend` (CharField)
- `dataset_version` (CharField)
- `benchmark_score` (FloatField)
- `is_active` (BooleanField)
- `metadata_json` (TextField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AIPrompt` (Table: `core_aiprompt`) 
**Description**: Centralized reusable prompt entity for WealthFlow AI Workspace.

#### Fields:
- `id` (BigAutoField)
- `user` (ForeignKey -> User): FK relation to `User`.
- `name` (CharField)
- `content` (TextField)
- `category` (ForeignKey -> AIPromptCategory): FK relation to `AIPromptCategory`.
- `description` (TextField)
- `translation_key` (CharField)
- `is_favorite` (BooleanField)
- `is_active` (BooleanField)
- `display_order` (IntegerField)
- `usage_count` (IntegerField)
- `last_used_at` (DateTimeField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AIPromptCategory` (Table: `core_aipromptcategory`) 
**Description**: First-class category for AI Workspace prompts.

#### Fields:
- `id` (BigAutoField)
- `code` (CharField)
- `name` (CharField)
- `description` (TextField)
- `icon` (CharField)
- `display_order` (IntegerField)
- `is_active` (BooleanField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AppSettings` (Table: `core_appsettings`) 
**Description**: AppSettings(id, key, value, description)

#### Fields:
- `id` (BigAutoField)
- `key` (CharField)
- `value` (TextField)
- `description` (CharField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetAcquisitionCost` (Table: `core_assetacquisitioncost`) 
**Description**: AssetAcquisitionCost(id, asset, date, category, description, amount_egp, usd_rate, amount_usd, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `date` (DateField)
- `category` (CharField)
- `description` (CharField)
- `amount_egp` (DecimalField)
- `usd_rate` (DecimalField)
- `amount_usd` (DecimalField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetFurniture` (Table: `core_assetfurniture`) 
**Description**: AssetFurniture(id, asset, name, category, purchase_date, amount_egp, usd_rate, amount_usd, quantity, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `name` (CharField)
- `category` (CharField)
- `purchase_date` (DateField)
- `amount_egp` (DecimalField)
- `usd_rate` (DecimalField)
- `amount_usd` (DecimalField)
- `quantity` (PositiveIntegerField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetInsurance` (Table: `core_assetinsurance`) 
**Description**: AssetInsurance(id, asset, company, policy_number, expiry_date, premium, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `company` (CharField)
- `policy_number` (CharField)
- `expiry_date` (DateField)
- `premium` (DecimalField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetMaintenance` (Table: `core_assetmaintenance`) 
**Description**: AssetMaintenance(id, asset, date, maintenance_type, cost, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `date` (DateField)
- `maintenance_type` (CharField)
- `cost` (DecimalField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetMortgage` (Table: `core_assetmortgage`) 
**Description**: AssetMortgage(id, asset, loan_amount, remaining_balance, monthly_installment, interest_rate, start_date, end_date, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `loan_amount` (DecimalField)
- `remaining_balance` (DecimalField)
- `monthly_installment` (DecimalField)
- `interest_rate` (DecimalField)
- `start_date` (DateField)
- `end_date` (DateField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetPhoto` (Table: `core_assetphoto`) 
**Description**: AssetPhoto(id, asset, image_data, filename, mime_type, title, uploaded_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `image_data` (BinaryField)
- `filename` (CharField)
- `mime_type` (CharField)
- `title` (CharField)
- `uploaded_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetPurchasePayment` (Table: `core_assetpurchasepayment`) 
**Description**: AssetPurchasePayment(id, asset, currency, payment_method, bank, amount, created_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `payment_method` (CharField)
- `bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `amount` (DecimalField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetRenovation` (Table: `core_assetrenovation`) 
**Description**: AssetRenovation(id, asset, date, category, description, amount_egp, usd_rate, amount_usd, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `date` (DateField)
- `category` (CharField)
- `description` (CharField)
- `amount_egp` (DecimalField)
- `usd_rate` (DecimalField)
- `amount_usd` (DecimalField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetRental` (Table: `core_assetrental`) 
**Description**: AssetRental(id, asset, monthly_rent, occupancy_rate, tenant_name, contract_start, contract_end, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `monthly_rent` (DecimalField)
- `occupancy_rate` (DecimalField)
- `tenant_name` (CharField)
- `contract_start` (DateField)
- `contract_end` (DateField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetSale` (Table: `core_assetsale`) 
**Description**: AssetSale(id, asset, sale_date, sale_price, selling_expenses, net_sale_amount, deposit_balance, deposit_currency, deposit_method, deposit_bank, notes, created_at)

#### Fields:
- `id` (BigAutoField)
- `sale_date` (DateField)
- `sale_price` (DecimalField)
- `selling_expenses` (DecimalField)
- `net_sale_amount` (DecimalField)
- `deposit_balance` (ForeignKey -> BalanceEntry): FK relation to `BalanceEntry`.
- `deposit_currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `deposit_method` (CharField)
- `deposit_bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `notes` (TextField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AssetValuationHistory` (Table: `core_assetvaluationhistory`) 
**Description**: AssetValuationHistory(id, asset, valuation_date, market_value, valuation_source, notes, created_at)

#### Fields:
- `id` (BigAutoField)
- `asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `valuation_date` (DateField)
- `market_value` (DecimalField)
- `valuation_source` (CharField)
- `notes` (TextField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AuthAuditLog` (Table: `core_authauditlog`) 
**Description**: AuthAuditLog(id, user, actor, event_type, details, created_at)

#### Fields:
- `id` (BigAutoField)
- `user` (ForeignKey -> User): FK relation to `User`.
- `actor` (ForeignKey -> User): FK relation to `User`.
- `event_type` (CharField)
- `details` (TextField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `AuthToken` (Table: `core_authtoken`) 
**Description**: AuthToken(id, user, purpose, token_hash, expires_at, used_at, created_at)

#### Fields:
- `id` (BigAutoField)
- `user` (ForeignKey -> User): FK relation to `User`.
- `purpose` (CharField)
- `token_hash` (CharField)
- `expires_at` (DateTimeField)
- `used_at` (DateTimeField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `BalanceEntry` (Table: `core_balanceentry`) 
**Description**: BalanceEntry(id, title, balance_type, bank, currency, purity, amount, entry_date, notes)

#### Fields:
- `id` (BigAutoField)
- `title` (CharField)
- `balance_type` (CharField)
- `bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `purity` (CharField)
- `amount` (DecimalField)
- `entry_date` (DateField)
- `notes` (TextField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `BalanceTransfer` (Table: `core_balancetransfer`) 
**Description**: BalanceTransfer(id, transfer_date, transfer_type, from_bank, to_bank, currency, amount, fee, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `transfer_date` (DateField)
- `transfer_type` (CharField)
- `from_bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `to_bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `amount` (DecimalField)
- `fee` (DecimalField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `Bank` (Table: `core_bank`) 
**Description**: Bank(id, name, account_number, card_id, swift_code, customer_id, customer_name, is_active, order)

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `account_number` (CharField)
- `card_id` (CharField)
- `swift_code` (CharField)
- `customer_id` (CharField)
- `customer_name` (CharField)
- `is_active` (BooleanField)
- `order` (IntegerField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `BankCertificate` (Table: `core_bankcertificate`) 
**Description**: BankCertificate(id, bank, currency, issue_date, expiry_date, amount, interest_rate, interest_value, frequency, status, last_interest_posted_date, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `issue_date` (DateField)
- `expiry_date` (DateField)
- `amount` (DecimalField)
- `interest_rate` (DecimalField)
- `interest_value` (DecimalField)
- `frequency` (CharField)
- `status` (CharField)
- `last_interest_posted_date` (DateField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `BankCertificateInterestHistory` (Table: `core_bankcertificateinteresthistory`) 
**Description**: BankCertificateInterestHistory(id, certificate, posting_date, posting_period, interest_amount, bank, currency, created_at)

#### Fields:
- `id` (BigAutoField)
- `certificate` (ForeignKey -> BankCertificate): FK relation to `BankCertificate`.
- `posting_date` (DateField)
- `posting_period` (CharField)
- `interest_amount` (DecimalField)
- `bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `CertificateStatus` (Table: `core_certificatestatus`) 
**Description**: Admin-configurable certificate lifecycle statuses.

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `color_hex` (CharField)
- `is_default` (BooleanField) — Used as default status for new certs
- `is_terminal` (BooleanField) — No further renewals expected
- `order` (IntegerField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `Company` (Table: `core_company`) 
**Description**: Company(id, name, display_name, group_name, color_hex, is_active, order, created_at, current_salary_amount, current_salary_currency, payment_day, default_bank, per_diem_amount, per_diem_currency, bonus_amount, payroll_notes, updated_at)

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `display_name` (CharField)
- `group_name` (CharField)
- `color_hex` (CharField)
- `is_active` (BooleanField)
- `order` (IntegerField)
- `created_at` (DateTimeField)
- `current_salary_amount` (DecimalField)
- `current_salary_currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `payment_day` (IntegerField)
- `default_bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `per_diem_amount` (DecimalField)
- `per_diem_currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `bonus_amount` (DecimalField)
- `payroll_notes` (TextField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `Currency` (Table: `core_currency`) 
**Description**: Currency(id, code, symbol, flag, name, order, created_at)

#### Fields:
- `id` (BigAutoField)
- `code` (CharField)
- `symbol` (CharField)
- `flag` (CharField)
- `name` (CharField)
- `order` (IntegerField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `CurrencyExchange` (Table: `core_currencyexchange`) 
**Description**: CurrencyExchange(id, exchange_date, from_balance, to_balance, from_currency, to_currency, from_amount, to_amount, exchange_rate, status, notes, user, reversed_at, reversed_by, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `exchange_date` (DateField)
- `from_balance` (ForeignKey -> BalanceEntry): FK relation to `BalanceEntry`.
- `to_balance` (ForeignKey -> BalanceEntry): FK relation to `BalanceEntry`.
- `from_currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `to_currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `from_amount` (DecimalField)
- `to_amount` (DecimalField)
- `exchange_rate` (DecimalField)
- `status` (CharField)
- `notes` (TextField)
- `user` (ForeignKey -> User): FK relation to `User`.
- `reversed_at` (DateTimeField)
- `reversed_by` (ForeignKey -> User): FK relation to `User`.
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `Document` (Table: `core_document`) 
**Description**: Document(id, parent_object_type, content_type, object_id, document_category, original_file_name, mime_type, file_size, file_content, file_hash, upload_date, uploaded_by, notes)

#### Fields:
- `id` (BigAutoField)
- `parent_object_type` (CharField)
- `content_type` (ForeignKey -> ContentType): FK relation to `ContentType`.
- `object_id` (PositiveIntegerField)
- `document_category` (CharField)
- `original_file_name` (CharField)
- `mime_type` (CharField)
- `file_size` (PositiveIntegerField)
- `file_content` (BinaryField)
- `file_hash` (CharField)
- `upload_date` (DateTimeField)
- `uploaded_by` (ForeignKey -> User): FK relation to `User`.
- `notes` (TextField)
- `parent_object` (GenericForeignKey -> Unknown): FK relation to `Unknown`.

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `DocumentationExecution` (Table: `core_documentationexecution`) 
**Description**: DocumentationExecution(id, started_at, finished_at, execution_type, language, theme, device_category, device_type, status, screenshots_count, failed_pages, pages, warnings, errors, missing_content_count, files_generated, git_commit_hash, app_version, engine_version, created_by)

#### Fields:
- `id` (BigAutoField)
- `started_at` (DateTimeField)
- `finished_at` (DateTimeField)
- `execution_type` (CharField)
- `language` (CharField)
- `theme` (CharField)
- `device_category` (CharField)
- `device_type` (CharField)
- `status` (CharField)
- `screenshots_count` (IntegerField)
- `failed_pages` (IntegerField)
- `pages` (IntegerField)
- `warnings` (JSONField)
- `errors` (JSONField)
- `missing_content_count` (IntegerField)
- `files_generated` (JSONField)
- `git_commit_hash` (CharField)
- `app_version` (CharField)
- `engine_version` (CharField)
- `created_by` (ForeignKey -> User): FK relation to `User`.

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `EmailTemplate` (Table: `core_emailtemplate`) 
**Description**: EmailTemplate(id, key, subject_translations, body_translations, description_translations, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `key` (CharField)
- `subject_translations` (JSONField)
- `body_translations` (JSONField)
- `description_translations` (JSONField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `ExchangeRate` (Table: `core_exchangerate`) 
**Description**: One row per currency per fetch. Latest row = current rate.

#### Fields:
- `id` (BigAutoField)
- `currency_code` (CharField)
- `currency_name` (CharField)
- `buy_rate` (DecimalField)
- `sell_rate` (DecimalField)
- `mid_rate` (DecimalField)
- `source` (CharField)
- `fetched_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `ExchangeRateHistory` (Table: `core_exchangeratehistory`) 
**Description**: Analytical-only archive of exchange rate snapshots.

#### Fields:
- `id` (BigAutoField)
- `currency_code` (CharField)
- `currency_name` (CharField)
- `buy_rate` (DecimalField)
- `sell_rate` (DecimalField)
- `mid_rate` (DecimalField)
- `source` (CharField)
- `fetched_at` (DateTimeField)
- `archived_at` (DateTimeField)
- `snapshot_date` (DateField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `Expense` (Table: `core_expense`) 
**Description**: Expense(id, date, year, month, category, subcategory, description, amount, exchange_rate, amount_egp, currency, bank, payment_method, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `date` (DateField)
- `year` (IntegerField)
- `month` (IntegerField)
- `category` (ForeignKey -> ExpenseCategory): FK relation to `ExpenseCategory`.
- `subcategory` (ForeignKey -> ExpenseSubcategory): FK relation to `ExpenseSubcategory`.
- `description` (CharField)
- `amount` (DecimalField)
- `exchange_rate` (DecimalField)
- `amount_egp` (DecimalField)
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `payment_method` (CharField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `ExpenseCategory` (Table: `core_expensecategory`) 
**Description**: ExpenseCategory(id, name, icon, color_hex, order, created_at)

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `icon` (CharField)
- `color_hex` (CharField)
- `order` (IntegerField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `ExpenseSubcategory` (Table: `core_expensesubcategory`) 
**Description**: ExpenseSubcategory(id, category, name, order, created_at)

#### Fields:
- `id` (BigAutoField)
- `category` (ForeignKey -> ExpenseCategory): FK relation to `ExpenseCategory`.
- `name` (CharField)
- `order` (IntegerField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `FixedAsset` (Table: `core_fixedasset`) 
**Description**: FixedAsset(id, name, asset_type, status, purchase_date, purchase_price, purchase_usd_rate, purchase_price_usd, current_market_value, valuation_source, last_valuation_date, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `asset_type` (CharField)
- `status` (CharField)
- `purchase_date` (DateField)
- `purchase_price` (DecimalField)
- `purchase_usd_rate` (DecimalField)
- `purchase_price_usd` (DecimalField)
- `current_market_value` (DecimalField)
- `valuation_source` (CharField)
- `last_valuation_date` (DateField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `Goal` (Table: `core_goal`) 
**Description**: Goal(id, name, goal_type, target_amount, currency, target_date, current_saved_amount, linked_asset, priority, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `goal_type` (CharField)
- `target_amount` (DecimalField)
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `target_date` (DateField)
- `current_saved_amount` (DecimalField)
- `linked_asset` (ForeignKey -> FixedAsset): FK relation to `FixedAsset`.
- `priority` (CharField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `GoldDetails` (Table: `core_golddetails`) 
**Description**: GoldDetails(id, asset, gold_type, purity, weight, unit, market_price, cashback_per_gram, purchase_weight, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `gold_type` (CharField)
- `purity` (CharField)
- `weight` (DecimalField)
- `unit` (CharField)
- `market_price` (DecimalField)
- `cashback_per_gram` (DecimalField)
- `purchase_weight` (DecimalField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `GoldPrice` (Table: `core_goldprice`) 
**Description**: One row per fetch. Stores EGP price per gram for common carats.

#### Fields:
- `id` (BigAutoField)
- `carat_24k` (DecimalField)
- `carat_22k` (DecimalField)
- `carat_21k` (DecimalField)
- `carat_18k` (DecimalField)
- `carat_24k_buy` (DecimalField)
- `carat_22k_buy` (DecimalField)
- `carat_21k_buy` (DecimalField)
- `carat_18k_buy` (DecimalField)
- `usd_gram_24k` (DecimalField)
- `usd_per_oz` (DecimalField)
- `usd_to_egp` (DecimalField)
- `source_gold` (CharField)
- `source_fx` (CharField)
- `fetched_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `GoldPriceHistory` (Table: `core_goldpricehistory`) 
**Description**: GoldPriceHistory(id, timestamp, carat_24k, carat_21k, carat_18k, usd_gram_24k, usd_per_oz, usd_to_egp)

#### Fields:
- `id` (BigAutoField)
- `timestamp` (DateTimeField)
- `carat_24k` (DecimalField)
- `carat_21k` (DecimalField)
- `carat_18k` (DecimalField)
- `usd_gram_24k` (DecimalField)
- `usd_per_oz` (DecimalField)
- `usd_to_egp` (DecimalField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `GoldPuritySetting` (Table: `core_goldpuritysetting`) 
**Description**: GoldPuritySetting(id, key, label, cashback_per_gram, is_active, order, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `key` (CharField)
- `label` (CharField)
- `cashback_per_gram` (DecimalField)
- `is_active` (BooleanField)
- `order` (IntegerField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `GoldTypeSetting` (Table: `core_goldtypesetting`) 
**Description**: GoldTypeSetting(id, name, is_active, order, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `is_active` (BooleanField)
- `order` (IntegerField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `OtherAssetDetails` (Table: `core_otherassetdetails`) 
**Description**: OtherAssetDetails(id, asset, category, manufacturer, model, serial_number, description, warranty_expiry, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `category` (CharField)
- `manufacturer` (CharField)
- `model` (CharField)
- `serial_number` (CharField)
- `description` (TextField)
- `warranty_expiry` (DateField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `PagePermission` (Table: `core_pagepermission`) 
**Description**: PagePermission(id, user, page, created_at)

#### Fields:
- `id` (BigAutoField)
- `user` (ForeignKey -> User): FK relation to `User`.
- `page` (CharField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `PerDiem` (Table: `core_perdiem`) 
**Description**: PerDiem(id, company, year, date, currency, amount, amount_egp, bank, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `company` (ForeignKey -> Company): FK relation to `Company`.
- `year` (IntegerField)
- `date` (DateField)
- `currency` (ForeignKey -> Currency): FK relation to `Currency`.
- `amount` (DecimalField)
- `amount_egp` (DecimalField)
- `bank` (ForeignKey -> Bank): FK relation to `Bank`.
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `RealEstateDetails` (Table: `core_realestatedetails`) 
**Description**: RealEstateDetails(id, asset, country, governorate, city, district, full_address, latitude, longitude, area_m2, bedrooms, bathrooms, floor_number, building_floors, build_year, has_elevator, has_garage, has_gas, electricity_meter_private, water_meter_private, has_land_share, land_share_ratio, facing, licensed, land_share_sqm, description, finishing_level, last_estimated_market_price, last_valuation_date, valuation_provider, furnished_status, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `country` (CharField)
- `governorate` (CharField)
- `city` (CharField)
- `district` (CharField)
- `full_address` (TextField)
- `latitude` (DecimalField)
- `longitude` (DecimalField)
- `area_m2` (DecimalField)
- `bedrooms` (PositiveSmallIntegerField)
- `bathrooms` (PositiveSmallIntegerField)
- `floor_number` (PositiveSmallIntegerField)
- `building_floors` (PositiveSmallIntegerField)
- `build_year` (PositiveSmallIntegerField)
- `has_elevator` (BooleanField)
- `has_garage` (BooleanField)
- `has_gas` (BooleanField)
- `electricity_meter_private` (BooleanField)
- `water_meter_private` (BooleanField)
- `has_land_share` (BooleanField)
- `land_share_ratio` (CharField)
- `facing` (CharField)
- `licensed` (BooleanField)
- `land_share_sqm` (DecimalField)
- `description` (TextField)
- `finishing_level` (CharField)
- `last_estimated_market_price` (DecimalField)
- `last_valuation_date` (DateField)
- `valuation_provider` (CharField)
- `furnished_status` (CharField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `ReminderLog` (Table: `core_reminderlog`) 
**Description**: Records each time a reminder was shown to avoid daily duplicates.

#### Fields:
- `id` (BigAutoField)
- `rule` (ForeignKey -> ReminderRule): FK relation to `ReminderRule`.
- `related_model` (CharField)
- `related_id` (IntegerField)
- `fired_on` (DateField)
- `message` (TextField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `ReminderRule` (Table: `core_reminderrule`) 
**Description**: Fully configurable reminder rule — no hardcoded values.

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `rule_type` (CharField)
- `is_active` (BooleanField)
- `days_before` (IntegerField) — Days before expiry (cert_maturity)
- `salary_trigger` (CharField)
- `salary_day` (IntegerField) — Trigger value for salary reminder
- `salary_message` (CharField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `SalaryEntry` (Table: `core_salaryentry`) 
**Description**: SalaryEntry(id, company, year, month, expected, paid, bonus, notes, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `company` (ForeignKey -> Company): FK relation to `Company`.
- `year` (IntegerField)
- `month` (CharField)
- `expected` (DecimalField)
- `paid` (DecimalField)
- `bonus` (DecimalField)
- `notes` (TextField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property
- `remaining` (property): Remaining = Expected - Paid, but never below 0 (per Excel logic).

### Model: `Scenario` (Table: `core_scenario`) 
**Description**: Scenario(id, name, description, is_baseline_pinned, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `name` (CharField)
- `description` (TextField)
- `is_baseline_pinned` (BooleanField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `ScenarioEvent` (Table: `core_scenarioevent`) 
**Description**: ScenarioEvent(id, scenario, event_type, event_date, params, order, created_at)

#### Fields:
- `id` (BigAutoField)
- `scenario` (ForeignKey -> Scenario): FK relation to `Scenario`.
- `event_type` (CharField)
- `event_date` (DateField)
- `params` (JSONField)
- `order` (PositiveIntegerField)
- `created_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `UserProfile` (Table: `core_userprofile`) 
**Description**: UserProfile(id, user, full_name, avatar_b64, bio, birthday, email_verified, account_status, status_reason, preferred_language, approved_at, approved_by, rejected_at, rejected_by, disabled_at, disabled_by, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `full_name` (CharField)
- `avatar_b64` (TextField)
- `bio` (TextField)
- `birthday` (DateField)
- `email_verified` (BooleanField)
- `account_status` (CharField)
- `status_reason` (TextField)
- `preferred_language` (CharField)
- `approved_at` (DateTimeField)
- `approved_by` (ForeignKey -> User): FK relation to `User`.
- `rejected_at` (DateTimeField)
- `rejected_by` (ForeignKey -> User): FK relation to `User`.
- `disabled_at` (DateTimeField)
- `disabled_by` (ForeignKey -> User): FK relation to `User`.
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property

### Model: `VehicleDetails` (Table: `core_vehicledetails`) 
**Description**: VehicleDetails(id, asset, brand, model, year, vin, engine, transmission, fuel_type, mileage, plate_number, license_expiry_date, color, created_at, updated_at)

#### Fields:
- `id` (BigAutoField)
- `brand` (CharField)
- `model` (CharField)
- `year` (PositiveSmallIntegerField)
- `vin` (CharField)
- `engine` (CharField)
- `transmission` (CharField)
- `fuel_type` (CharField)
- `mileage` (DecimalField)
- `plate_number` (CharField)
- `license_expiry_date` (DateField)
- `color` (CharField)
- `created_at` (DateTimeField)
- `updated_at` (DateTimeField)

#### Calculated Properties:
- `pk` (property): Calculated property
